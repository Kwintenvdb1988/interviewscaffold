FROM alpine:latest

# List all files in the build context
RUN ls -la /workspace/

# Try to list frontend directory specifically
RUN ls -la /workspace/frontend/ || echo "frontend directory not found"

# Try to list frontend package files
RUN ls -la /workspace/frontend/package*.json || echo "package files not found"

# Show the working directory
RUN pwd && ls -la 